# DSH Web customization and personal ZenTao CLI work center.
# Install this bundle after dsh-web-app; both rows are additive and disappear
# together when the bundle is removed from the profile.

- insert:
    - id: zentao-cli-gateway
      name: '@haoyu-qi/dsh-host-zentao-cli-gateway'

    - id: ui-zentao-notifications
      name: '@haoyu-qi/dsh-client-ui-zentao-notifications'
